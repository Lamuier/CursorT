# CursorUsage 开发约定

面向在本仓库工作的 AI / 开发助手。改代码前先读本文与 `README.md`。

- 单模块原生 Android 应用（Kotlin + Jetpack Compose + Material 3）
- 包名 `com.lamuier.cursorusage`（重品牌后统一；改动包名会使 Android 视为不同应用，已装用户无法收到更新且丢失本机加密账号，非必要不改动）
- 构建入口：`.\build.ps1` / `.\build.ps1 -Install` / `.\build.ps1 -Release` / `.\build.ps1 -SetupSigning`
- 用户沟通默认用**简体中文**

## 改动后别忘了变更记录

功能变更必须同步更新 `CHANGELOG.md`，但**不要默认每次对话/每个小改动都升级版本号**。

默认规则：同一批需求、同一次对话内的连续修改、或未明确发布的增量更新，都合并记录到 `CHANGELOG.md` 顶部的未归档记录中；如果未归档记录不存在，先在最顶部创建，不要直接塞进已发布版本条目。

只有用户明确要求“归档/升版本/发版/发布新版本”时，才把未归档记录整理为新的版本条目，并更新版本号。需要更新版本号时必须保持全仓一致：至少同步检查并更新 `app/build.gradle.kts` 的 `versionName` / `versionCode`、`README.md` 当前版本说明，以及 `CHANGELOG.md` 对应版本条目；若仓库存在根目录 `VERSION` 文件也一并更新。改完必须用 `rg` 或等效命令自查旧版本号是否仍残留在当前版本声明位置；历史变更记录中的旧版本号不要为了“统一”而改写。

### 版本级别判定（SemVer，发版前必须执行）

版本号为 `X.Y.Z`，必须按本批改动中**影响最高**的内容定级，禁止机械地只升级 `Z`：

| 级别 | 适用范围 | 典型示例 |
| --- | --- | --- |
| `X` 主版本 | 存在不向后兼容的接口、数据语义、权限或部署变更，需要调用方、用户或运维主动适配 | 删除/重命名公开 API，改变既有字段含义，移除核心能力，不兼容的配置格式、数据库 Schema 或安装包签名要求 |
| `Y` 次版本 | 向后兼容地新增用户可见能力或业务契约 | 新增页面、小组件、权限项、API、数据库表/字段、业务模块、可配置流程或完整交互功能 |
| `Z` 修订版 | 不新增业务能力的向后兼容修复与维护 | Bug 修复、性能优化、样式微调、内部重构、依赖维护、文档修订 |

- 同一批同时包含新增、优化和修复时，按最高级别发布；例如“新增页面 + 性能优化”必须升级 `Y`，不能因为也包含修复就只升级 `Z`。
- 改动量大小、是否发生在同一次对话、是否沿用已有模块，都不能作为降低版本级别的理由；判断依据是对外能力与兼容性。
- 发版计划中必须先明确写出“版本判定：当前版本 → 目标版本（依据）”，再修改版本文件。无法明确判级时先向用户确认，不得默认选择修订版。
- Android 发版时 `versionCode` 必须随新版本递增；`versionName` 遵循上述 SemVer。
- 已推送远端的版本 tag 默认视为不可变历史，不要删除、强推或改指向；若发现定级错误，优先发布新的纠正版本并在 `CHANGELOG.md` 说明。只有用户明确授权改写远端历史时才可例外处理。

如果用户明确要求提交发布相关改动，提交后必须创建对应版本的 Git tag（例如 `v1.10.1`）；除非用户明确要求推送，否则不要执行 `git push` 或推送 tag。
